# Port for the backend server
PORT=3000

# Crossref recommends providing a contact email; set it here
CROSSREF_MAILTO=your-email@example.com

# Max rows per request (backend enforces a safe max)
MAX_ROWS=25